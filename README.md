# lit-element build repo

## build a new version

```
npm i -S lit-element@latest lit-html@latest
polymer build
```

## upload

publication is done by a git action... 

so just `git commit` and `git push`...
